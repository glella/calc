[package]
name = "calc"
version = "0.1.0"
build = "build.rs"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
quit = "2.0.0"
slint = "1.0.0"

[build-dependencies]
slint-build = "1.0.0"
